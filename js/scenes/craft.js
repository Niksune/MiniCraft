var Craft = new Phaser.Class({

    Extends: Phaser.Scene,

    initialize:

        function Craft() {
            Phaser.Scene.call(this, { key: 'Craft' });
        },

    preload: function () {
    },

    create: function () {

        Craft = this.scene.get("Craft");

        this.



    }
});